// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
// };

// export default nextConfig;



const nextConfig = {
  reactStrictMode: true,
  output: "export", // ✅ 추가!
};

export default nextConfig;